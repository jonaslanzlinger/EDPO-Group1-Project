# Plotter
This is a simple plotter that uses the matplotlib library to plot the recorded mqtt data
from the factory over time.

## Usage
Run the python script or the jupyter notebook to plot the data.